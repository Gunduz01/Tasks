// Tabs
// function showTab(index) {
//   const tabs = document.querySelectorAll(".tab");
//   tabs.forEach((tab, i) => {
//     tab.classList.toggle("active", i === index);
//   });
// }

// Create a set of tabs that display different content when clicked

// function showTab(index) {
//   const tabs = document.querySelectorAll(".tab");
//   const buttons = document.querySelectorAll(".tab-button");

//   tabs.forEach((tab, i) => {
//     tab.classList.toggle("active", i === index);
//     buttons[i].classList.toggle("active", i === index);
//   });
// }

// function showTab(index) {
//   const tabs = document.querySelectorAll(".tab");            // Tüm tab içerikleri seçilir
//   const buttons = document.querySelectorAll(".tab-button");   // Tüm tab butonları seçilir

//   tabs.forEach((tab, i) => {
//     tab.classList.toggle("active", i === index);              // Sadece tıklanan indexteki içerik görünür
//     buttons[i].classList.toggle("active", i === index);       // Sadece tıklanan buton aktif görünür
//   });
// }

// addEventListener("click",showTab)


